# later/

These are stage 2–3 of the build order in the main document — a gate and a memory hook, not part of the mandatory stage 1. Copy them only after the failure the document names — and if you already have a `.claude/settings.json`, merge the `hooks` block into it rather than replacing the file: the gate after a rule in CLAUDE.md has been ignored at least once, the memory hook after you have re-explained the same project twice.
